# Lab 05 — Ship the MCP Server, Pass the Evals
1. `dotnet run --project ../../../src/DataMapping.Mcp` (stdio) and connect an MCP client.
2. Implement the TODO tools in McpToolHandlers.cs.
3. `dotnet run --project ../../../src/DataMapping.Evals` and iterate until the scorecard is green.
