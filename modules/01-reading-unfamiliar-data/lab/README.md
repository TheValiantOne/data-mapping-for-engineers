# Lab 01 — Build the Data Dossier
Run the profiler skeleton in `src/DataMapping.Core` (Profiling namespace) against `data/generated/`. Extend it where the TODOs indicate. Produce `dossier.md` in this folder.
